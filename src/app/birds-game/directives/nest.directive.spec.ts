import { NestDirective } from './nest.directive';

describe('NestDirective', () => {
  it('should create an instance', () => {
    const directive = new NestDirective();
    expect(directive).toBeTruthy();
  });
});
