import { BaseBodyDirective } from './base-body.directive';

describe('BaseBodyDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseBodyDirective();
    expect(directive).toBeTruthy();
  });
});
